import './PublicAppPrototype1Layout.scss'

export default function PublicAppPrototype1Layout({
    defaultViewName,
    children,
}) {
    return <div className="PublicAppPrototype1Layout">{children}</div>
}
